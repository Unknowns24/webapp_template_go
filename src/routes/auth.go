package routes

import (
	"github.com/labstack/echo/v4"
)

func CreateAuthRoutes(app *echo.Echo) {
	/*
		// Logout route
		app.POST("logout")

		// Login routes
		app.GET("login")

		app.POST("login")

		// Register routes
		app.GET("register")

		app.POST("register")

		// Reset password routes
		app.GET("resetPassword")

		app.GET("resetPassword/{userid}/{token}")

		app.POST("resetPassword/{userid}/{token}")

		// Mail verification routes
		app.GET("verification/mail")

		app.GET("verification/mail/resend")

		app.GET("verificate/mail/{userid}/{token}")
	*/
}
